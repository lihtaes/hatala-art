<section id="portfolio-work">
        <div class="container">
            <div class="row">
              <div class="col-md-12">
                <div class="block">
                  <div class="portfolio-contant">
                    <ul id="portfolio-contant-active">
                        <% @vessels.each do |vessel| %>
                        <li class="animated fadeInUp <%= vessel.category %>">
                          <a href="#">
                            <img src="<%= vessel.image %>" alt="">
                            <div class="overly">
                              <div class="position-center ">
                                <h2 class="align-center"><%= vessel.title %></h2>
                                <p class="align-center"><%= vessel.subtitle %></p>
                              </div>
                            </div>
                          </a>
                      </li>
                      <% end %>
                    </ul>
                  </div>
                </div>
              </div>
            </div>
        </div>
    </section>